###############################################
# Ambiente de Exemplo (.env.example)
# Copie para .env e ajuste conforme necessário
###############################################

# Host do banco (nome do serviço no docker-compose)
DB_HOST=db

# Credenciais de aplicação / banco
DB_USER=user_desafio
DB_PASSWORD=senha_forte_123
DB_NAME=desafio_db

# Porta interna do Postgres (não altere normalmente)
# DB_PORT=5432

# NODE_ENV pode ser 'production' ou 'development'
# NODE_ENV=production

# Ajuste o número de rounds do bcrypt (default 10 se omisso)
# BCRYPT_ROUNDS=10

# Para debug detalhado (descomente se precisar)
# DEBUG=express:*,pg:pool

# Ao usar docker-compose não é necessário definir POSTGRES_* aqui
# porque o compose mapeia DB_* -> POSTGRES_* no serviço db.
